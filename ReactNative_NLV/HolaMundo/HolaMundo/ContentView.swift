//
//  ContentView.swift
//  HolaMundo
//
//  Created by Nabi lagunes vela on 12/08/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hola Nabilovers!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
